import { Dialog, DialogContent, DialogTitle } from '@mui/material'



export default function ModalRead(open: Boolean) {
    return (
        <Dialog open={false}>
            <DialogTitle></DialogTitle>
            <DialogContent>

            </DialogContent>

        </Dialog>
    )
}